{{- $settings := includeTemplate "machine-settings" . | fromJson -}}
{{- $tunnels := index $settings "cloudflare_tunnels" -}}
{{- if and $tunnels .has_keepassxc_db -}}
{{ includeTemplate "sopsDecrypt" (merge (dict "file" "private_dot_cloudflared/cert.pem.sops") .) -}}
{{- end -}}