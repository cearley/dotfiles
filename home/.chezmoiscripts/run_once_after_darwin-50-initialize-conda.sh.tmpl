{{- if and (eq .chezmoi.os "darwin") (has "ai" .tags) (has "miniforge" .packages.darwin.ai.casks) -}}
#!/bin/bash

if command -v conda >/dev/null 2>&1; then
    echo "🐍 Initializing conda for zsh shell..."
    conda init zsh
else
    echo "⚠️  conda command not found - skipping initialization"
fi

{{ end -}}