{{- if and (eq .chezmoi.os "darwin") (has "ai" .tags) -}}
{
  "mcpServers": {
    "basic-memory": {
      "command": "{{- .chezmoi.homeDir -}}/.local/bin/uvx",
      "args": [
        "basic-memory",
        "mcp"
      ]
    }{{- if (has "work" .tags) -}},
    "jetbrains": {
      "command": "npx",
      "args": [
        "-y",
        "@jetbrains/mcp-proxy"
      ]
    }{{- end }}
  }
}
{{ end -}}